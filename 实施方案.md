# 文本内容管理与敏感信息审核系统 — 实施方案

## 一、项目概述

基于技术选型方案，本文档详细规划系统的实施步骤、模块开发顺序、接口规范与验收标准。

- **技术栈**：Node.js + Express + React 18 + Ant Design 5 + MySQL 8.0 + Prisma
- **团队规模**：4–5 人
- **总工期**：5 周

---

## 二、开发环境搭建

### 2.1 环境要求

| 工具 | 版本 | 用途 |
|------|------|------|
| Node.js | ≥18 LTS | 运行环境 |
| MySQL | 8.0 | 数据库 |
| Git | 最新 | 版本控制 |
| VS Code | 最新 | IDE |
| pnpm | ≥8 | 包管理器（推荐） |

### 2.2 项目初始化步骤

```bash
# 1. 创建项目根目录
mkdir text-guard && cd text-guard
git init

# 2. 初始化后端
mkdir -p server/src/{controllers,services,middlewares,engine,routes,utils,types}
cd server && pnpm init && pnpm add express prisma @prisma/client jsonwebtoken bcryptjs zod ahocorasick docx exceljs cors
pnpm add -D typescript ts-node-dev @types/express @types/jsonwebtoken @types/bcryptjs @types/cors
npx tsc --init
npx prisma init

# 3. 初始化前端
cd .. && npx -y create-vite@latest client -- --template react-ts
cd client && pnpm add antd @ant-design/icons zustand axios react-router-dom dayjs
pnpm add -D @types/react-router-dom
```

### 2.3 代码规范配置

```bash
# 根目录统一配置
pnpm add -D eslint prettier eslint-config-prettier
```

- ESLint：`@typescript-eslint` 规则集
- Prettier：统一格式（2空格、单引号、尾逗号）
- Git Hooks：`husky` + `lint-staged`，提交前自动格式化

---

## 三、数据库实施

### 3.1 Prisma Schema 定义

```prisma
// server/prisma/schema.prisma
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id           Int       @id @default(autoincrement())
  username     String    @unique @db.VarChar(50)
  passwordHash String    @map("password_hash") @db.VarChar(255)
  role         Role      @default(editor)
  createdAt    DateTime  @default(now()) @map("created_at")
  contents     Content[]
  reviews      Review[]
  logs         OperationLog[]

  @@map("users")
}

enum Role {
  admin
  editor
}

model Content {
  id        Int           @id @default(autoincrement())
  title     String        @db.VarChar(200)
  body      String        @db.Text
  category  String        @default("") @db.VarChar(50)
  status    ContentStatus @default(draft)
  riskLevel RiskLevel?    @map("risk_level")
  isPinned  Boolean       @default(false) @map("is_pinned")
  authorId  Int           @map("author_id")
  author    User          @relation(fields: [authorId], references: [id])
  reviews   Review[]
  createdAt DateTime      @default(now()) @map("created_at")
  updatedAt DateTime      @updatedAt @map("updated_at")

  @@index([status, createdAt])
  @@map("contents")
}

enum ContentStatus {
  draft
  pending
  published
  rejected
}

enum RiskLevel {
  low
  medium
  high
}

model SensitiveWord {
  id          Int       @id @default(autoincrement())
  word        String    @unique @db.VarChar(100)
  riskLevel   RiskLevel @map("risk_level")
  replacement String    @default("***") @db.VarChar(100)
  category    String    @default("") @db.VarChar(50)
  enabled     Boolean   @default(true)
  createdAt   DateTime  @default(now()) @map("created_at")

  @@index([enabled])
  @@map("sensitive_words")
}

model Review {
  id         Int      @id @default(autoincrement())
  contentId  Int      @map("content_id")
  content    Content  @relation(fields: [contentId], references: [id])
  reviewerId Int      @map("reviewer_id")
  reviewer   User     @relation(fields: [reviewerId], references: [id])
  action     ReviewAction
  comment    String?  @db.Text
  createdAt  DateTime @default(now()) @map("created_at")

  @@index([contentId])
  @@map("reviews")
}

enum ReviewAction {
  approve
  reject
}

model OperationLog {
  id         Int      @id @default(autoincrement())
  userId     Int      @map("user_id")
  user       User     @relation(fields: [userId], references: [id])
  action     String   @db.VarChar(50)
  targetType String   @map("target_type") @db.VarChar(50)
  targetId   Int?     @map("target_id")
  detail     String?  @db.Text
  ip         String?  @db.VarChar(45)
  createdAt  DateTime @default(now()) @map("created_at")

  @@index([userId, createdAt])
  @@map("operation_logs")
}
```

### 3.2 数据库迁移与种子数据

```bash
# 迁移
npx prisma migrate dev --name init

# 种子脚本 server/prisma/seed.ts
# 插入默认管理员、示例敏感词（50+常用词）、示例内容
npx prisma db seed
```

种子数据包含：
- 默认管理员账号 `admin / admin123`
- 默认编辑账号 `editor / editor123`
- 50+ 示例敏感词（分高/中/低三级）
- 5 篇示例内容（覆盖各状态）

---

## 四、后端 API 实施

### 4.1 项目结构

```
server/src/
├── app.ts                 # Express 应用入口
├── controllers/
│   ├── auth.controller.ts
│   ├── content.controller.ts
│   ├── sensitive.controller.ts
│   ├── review.controller.ts
│   ├── report.controller.ts
│   └── log.controller.ts
├── services/
│   ├── auth.service.ts
│   ├── content.service.ts
│   ├── sensitive.service.ts
│   ├── review.service.ts
│   ├── report.service.ts
│   └── log.service.ts
├── middlewares/
│   ├── auth.middleware.ts      # JWT 验证
│   ├── role.middleware.ts      # RBAC 权限
│   ├── logger.middleware.ts    # 操作日志记录
│   └── validate.middleware.ts  # Zod 参数校验
├── engine/
│   ├── detector.ts             # AC 自动机检测器
│   ├── regex-rules.ts          # 正则规则引擎
│   ├── risk-scorer.ts          # 风险评分
│   └── filter.ts               # 内容过滤策略
├── routes/
│   └── index.ts                # 路由注册
├── utils/
│   ├── jwt.ts
│   ├── response.ts             # 统一响应格式
│   └── prisma.ts               # Prisma 客户端单例
└── types/
    └── index.ts
```

### 4.2 RESTful API 接口清单

#### 认证模块

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/auth/login` | 登录，返回 JWT | 公开 |
| POST | `/api/auth/register` | 注册（管理员创建用户） | admin |
| GET | `/api/auth/profile` | 获取当前用户信息 | 登录 |
| PUT | `/api/auth/password` | 修改密码 | 登录 |

#### 内容管理模块

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/contents` | 内容列表（分页、筛选） | 登录 |
| GET | `/api/contents/:id` | 内容详情 | 登录 |
| POST | `/api/contents` | 创建内容（自动检测） | editor |
| PUT | `/api/contents/:id` | 编辑内容 | editor(作者) |
| DELETE | `/api/contents/:id` | 删除内容 | editor(作者)/admin |
| PATCH | `/api/contents/:id/submit` | 提交审核 | editor(作者) |
| PATCH | `/api/contents/:id/pin` | 置顶/取消置顶 | admin |
| POST | `/api/contents/detect` | 预检测文本（不保存） | editor |

#### 敏感词库模块

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/sensitive-words` | 敏感词列表（分页） | admin |
| POST | `/api/sensitive-words` | 添加敏感词 | admin |
| POST | `/api/sensitive-words/batch` | 批量导入 | admin |
| PUT | `/api/sensitive-words/:id` | 修改敏感词 | admin |
| DELETE | `/api/sensitive-words/:id` | 删除敏感词 | admin |
| PATCH | `/api/sensitive-words/:id/toggle` | 启用/禁用 | admin |

#### 审核模块

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/reviews/pending` | 待审核列表 | admin |
| GET | `/api/reviews/history` | 审核历史 | admin |
| POST | `/api/reviews/:contentId/approve` | 通过审核 | admin |
| POST | `/api/reviews/:contentId/reject` | 驳回审核 | admin |

#### 报表模块

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/reports/stats` | 审核统计数据 | admin |
| GET | `/api/reports/export/word` | 导出 Word 报告 | admin |
| GET | `/api/reports/export/excel` | 导出 Excel 报表 | admin |

#### 操作日志模块

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/logs` | 日志列表（分页、筛选） | admin |

### 4.3 统一响应格式

```typescript
// 成功
{ code: 200, message: "success", data: { ... } }

// 分页
{ code: 200, message: "success", data: { list: [...], total: 100, page: 1, pageSize: 20 } }

// 错误
{ code: 401, message: "未授权", data: null }
```

### 4.4 敏感词检测引擎实施

**初始化流程**：
1. 服务启动时从数据库加载所有 `enabled=true` 的敏感词
2. 构建 AC 自动机实例（一次性构建）
3. 当敏感词库发生增删改时，重建自动机实例

**检测流程**：
1. 输入文本 → AC 自动机扫描（精确匹配）
2. 输入文本 → 正则引擎扫描（手机号、身份证等）
3. 合并命中结果 → 风险评分计算
4. 根据风险等级返回过滤策略建议

**过滤策略映射**：
- 低风险 (score < 30)：替换敏感词为 `***`，允许发布
- 中风险 (30 ≤ score < 60)：屏蔽内容 + 提示修改
- 高风险 (score ≥ 60)：拒绝发布，强制修改

---

## 五、前端实施

### 5.1 项目结构

```
client/src/
├── App.tsx                    # 根组件 + 路由
├── main.tsx                   # 入口
├── components/
│   ├── Layout/
│   │   ├── AppLayout.tsx      # 整体布局（侧边栏 + 头部）
│   │   └── AuthGuard.tsx      # 路由守卫
│   ├── RiskTag.tsx            # 风险等级标签
│   ├── StatusTag.tsx          # 内容状态标签
│   └── DetectResult.tsx       # 检测结果展示
├── pages/
│   ├── Login/                 # 登录页
│   ├── Dashboard/             # 工作台首页
│   ├── Content/
│   │   ├── ContentList.tsx    # 内容列表
│   │   ├── ContentForm.tsx    # 创建/编辑内容
│   │   └── ContentDetail.tsx  # 内容预览
│   ├── SensitiveWords/        # 敏感词库管理
│   ├── Review/
│   │   ├── ReviewList.tsx     # 审核工作台
│   │   └── ReviewDetail.tsx   # 审核详情
│   ├── Reports/               # 审核报表
│   └── Logs/                  # 操作日志
├── stores/
│   ├── useAuthStore.ts        # 认证状态
│   └── useAppStore.ts         # 全局 UI 状态
├── services/
│   ├── request.ts             # Axios 实例（拦截器）
│   ├── auth.api.ts
│   ├── content.api.ts
│   ├── sensitive.api.ts
│   ├── review.api.ts
│   ├── report.api.ts
│   └── log.api.ts
├── hooks/
│   ├── useAuth.ts
│   └── usePagination.ts
├── types/
│   └── index.ts
└── utils/
    └── index.ts
```

### 5.2 页面功能详述

#### 登录页 `/login`
- Ant Design Form 表单，用户名 + 密码
- 登录成功后 JWT 存入 `localStorage`，跳转 Dashboard

#### 工作台 `/dashboard`
- 统计卡片：今日发布数、待审核数、高风险内容数、本周审核通过率
- 最近操作日志（最新 10 条）
- 待审核内容快捷入口

#### 内容管理 `/contents`
- Ant Design Table 分页列表
- 筛选：状态、风险等级、分类、日期范围
- 操作：编辑、删除、提交审核、置顶
- 创建/编辑页：标题 + 富文本编辑 + 分类选择 + 实时检测预览

#### 敏感词库 `/sensitive-words`
- 表格管理：词条、风险等级、替换词、分类、启用状态
- 支持单条添加、批量导入（CSV/文本粘贴）
- 支持搜索、按等级/分类筛选

#### 审核工作台 `/review`
- 待审核队列列表
- 审核详情页：原文展示 + 敏感词高亮标注 + 风险评分
- 操作：通过 / 驳回（附审核意见）

#### 报表页 `/reports`
- 可视化图表（Ant Design Charts）：审核趋势、风险分布、分类占比
- 导出按钮：Word 审核报告 / Excel 数据报表

#### 操作日志 `/logs`
- 日志列表：操作人、行为、对象、时间、IP
- 筛选：操作类型、用户、日期范围

### 5.3 Axios 请求封装

```typescript
// services/request.ts
import axios from 'axios';
import { useAuthStore } from '@/stores/useAuthStore';

const request = axios.create({
  baseURL: '/api',
  timeout: 10000,
});

// 请求拦截：自动附加 Token
request.interceptors.request.use((config) => {
  const token = useAuthStore.getState().token;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// 响应拦截：统一错误处理
request.interceptors.response.use(
  (res) => res.data,
  (err) => {
    if (err.response?.status === 401) {
      useAuthStore.getState().logout();
      window.location.href = '/login';
    }
    return Promise.reject(err.response?.data || err);
  }
);

export default request;
```

---

## 六、模块开发顺序与里程碑

### 第 1 周：需求分析 + 环境搭建

| 任务 | 负责人 | 产出 |
|------|--------|------|
| 需求文档、用例图 | 全员 | 需求分析文档 |
| 搭建项目脚手架 | 后端 | 可运行的空项目 |
| 数据库 Schema 设计 | 后端 | Prisma Schema + 迁移 |
| 前端路由与布局骨架 | 前端 | 基本页面框架 |

### 第 2 周：核心模块开发

| 任务 | 负责人 | 产出 |
|------|--------|------|
| 认证模块（注册/登录/JWT） | 后端A | auth API |
| 内容 CRUD API | 后端A | content API |
| 敏感词检测引擎 | 后端B | detector 模块 |
| 登录页 + 布局 + 路由守卫 | 前端 | 基础页面 |
| 内容管理页面 | 前端 | 内容列表 + 表单 |

### 第 3 周：业务模块开发

| 任务 | 负责人 | 产出 |
|------|--------|------|
| 审核流程 API | 后端A | review API |
| 敏感词库 CRUD API | 后端B | sensitive-words API |
| 过滤策略 + 操作日志 | 后端B | filter + log 模块 |
| 敏感词管理页 + 审核工作台 | 前端 | 管理页面 |
| 前后端联调 | 全员 | 接口对接 |

### 第 4 周：报表与完善

| 任务 | 负责人 | 产出 |
|------|--------|------|
| 报表统计 API + 导出 | 后端B | report API |
| Dashboard + 报表页面 | 前端 | 可视化图表 |
| 操作日志页面 | 前端 | 日志页面 |
| Swagger 文档生成 | 后端A | API 文档 |
| 功能联调与 Bug 修复 | 全员 | 稳定系统 |

### 第 5 周：测试与交付

| 任务 | 负责人 | 产出 |
|------|--------|------|
| 功能测试 + 边界测试 | 测试 | 测试报告 |
| Bug 修复与优化 | 开发 | 稳定版本 |
| 实践报告撰写 | 测试 | 实践报告 |
| 答辩 PPT + 演示录制 | 全员 | PPT + 视频 |
| 部署上线（单机/云） | 后端 | 可演示系统 |

---

## 七、关键实现方案

### 7.1 JWT 认证流程

```
登录请求 → 验证用户名密码 → 生成 AccessToken(15min) + RefreshToken(7d)
                                    ↓
前端存储 Token → 请求头 Authorization: Bearer <token>
                                    ↓
中间件验证 → Token 过期 → 使用 RefreshToken 刷新 → 返回新 Token
```

### 7.2 RBAC 权限中间件

```typescript
// middlewares/role.middleware.ts
export const requireRole = (...roles: Role[]) => {
  return (req: Request, res: Response, next: NextFunction) => {
    if (!roles.includes(req.user.role)) {
      return res.status(403).json({ code: 403, message: '权限不足' });
    }
    next();
  };
};

// 使用示例
router.get('/sensitive-words', auth, requireRole('admin'), controller.list);
```

### 7.3 操作日志中间件

```typescript
// middlewares/logger.middleware.ts
export const logOperation = (action: string, targetType: string) => {
  return async (req: Request, res: Response, next: NextFunction) => {
    const originalJson = res.json.bind(res);
    res.json = (body: any) => {
      if (res.statusCode < 400) {
        prisma.operationLog.create({
          data: {
            userId: req.user.id,
            action,
            targetType,
            targetId: Number(req.params.id) || body?.data?.id,
            detail: JSON.stringify({ body: req.body }),
            ip: req.ip,
          },
        }).catch(console.error);
      }
      return originalJson(body);
    };
    next();
  };
};
```

### 7.4 报表导出实现

**Word 报告**（使用 `docx` 库）：
- 封面：报告标题、生成日期
- 统计概要：总内容数、审核通过率、风险分布
- 详细记录：高风险内容列表、命中敏感词

**Excel 报表**（使用 `exceljs` 库）：
- Sheet1：审核统计汇总
- Sheet2：内容明细数据
- Sheet3：敏感词命中统计

---

## 八、测试方案

### 8.1 后端接口测试

```bash
# 使用 Jest + Supertest
pnpm add -D jest ts-jest supertest @types/jest @types/supertest
```

**测试覆盖**：

| 模块 | 测试重点 |
|------|----------|
| 认证 | 登录成功/失败、Token 过期、权限拦截 |
| 内容 | CRUD 完整流程、状态流转、分页排序 |
| 检测引擎 | 敏感词命中、风险评分、过滤策略 |
| 审核 | 通过/驳回、重提交流转 |
| 边界 | 空内容、超长文本、SQL 注入尝试、XSS 尝试 |

### 8.2 前端测试

```bash
# 使用 Vitest + React Testing Library
pnpm add -D vitest @testing-library/react @testing-library/jest-dom
```

- 组件渲染测试
- 表单校验测试
- 路由守卫测试

---

## 九、部署实施

### 9.1 单机部署（答辩推荐）

```bash
# 1. 前端构建
cd client && pnpm build    # 输出到 client/dist/

# 2. Express 托管静态文件
app.use(express.static(path.join(__dirname, '../../client/dist')));
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, '../../client/dist/index.html'));
});

# 3. 启动服务
cd server && pnpm build && node dist/app.js
# 访问 http://localhost:3000
```

### 9.2 环境变量配置

```env
# server/.env
DATABASE_URL="mysql://root:password@localhost:3306/text_guard"
JWT_SECRET="your-secret-key"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"
PORT=3000
```

---

## 十、验收标准

| 功能 | 验收标准 |
|------|----------|
| 用户认证 | 登录/登出正常，JWT 鉴权生效，角色权限隔离 |
| 内容管理 | 发布、编辑、删除、分类、置顶、预览全流程通过 |
| 敏感词库 | 增删改查、批量导入、启用/禁用功能正常 |
| 智能检测 | AC 自动机精确匹配、正则匹配、风险评分准确 |
| 内容过滤 | 替换/屏蔽/提示/拒绝四种策略按风险等级生效 |
| 审核流程 | 提交→待审核→通过/驳回→重提交完整状态流转 |
| 操作日志 | 所有关键操作均有记录，支持筛选查询 |
| 报表导出 | Word 审核报告和 Excel 数据报表可正常下载 |
| 安全防护 | SQL 注入、XSS、CSRF 攻击均被拦截 |

---

## 十一、风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|:---:|:---:|----------|
| AC 自动机内存占用过大 | 低 | 中 | 限制敏感词库 1 万条以内，按需分批加载 |
| 前后端联调延迟 | 中 | 高 | Swagger 文档先行，Mock 数据并行开发 |
| MySQL 性能瓶颈 | 低 | 中 | 合理建索引，大表分页使用游标 |
| 成员技术能力差异 | 中 | 中 | 代码审查 + 结对编程，统一代码规范 |
| 答辩演示环境故障 | 中 | 高 | 提前录制演示视频备用 |
