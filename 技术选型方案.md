# 文本内容管理与敏感信息审核系统 — 技术选型
## 项目要求

课题22：文本内容管理与敏感信息审核系统
项目描述：本系统是内容管理系统+ 敏感内容智能检测融合项目，实现文本发布、审核、
敏感词过滤、风险预警、日志审计。
详细功能要求
内容管理：文本发布、编辑、删除、分类、置顶、预览。
敏感词库管理：自定义敏感词、风险等级、替换词。
智能检测：自动扫描内容，识别违规、敏感、风险语句。
内容过滤：支持替换、屏蔽、提示、拒绝发布策略。
审核流程：管理员审核、通过、驳回、修改重提交。
风险等级判定：低、中、高风险标记。
操作日志：记录发布、修改、审核、删除记录。
审核报表导出：生成内容安全审核报告。


## 一、项目概述

本系统是内容管理系统 + 敏感内容智能检测融合项目，实现文本发布、审核、敏感词过滤、风险预警、日志审计等功能。采用 B/S 前后端分离架构，全栈 TypeScript。

## 二、系统架构

```
┌──────────────────────────────────────────────────┐
│                  浏览器 (Browser)                  │
├──────────────────────────────────────────────────┤
│              React 18 前端 (SPA)                   │
│   ┌──────┬────────┬──────────┬──────┬─────────┐  │
│   │内容管理│敏感词库  │审核工作台  │报表   │用户管理  │  │
│   └──────┴────────┴──────────┴──────┴─────────┘  │
│          React Router 6 + Zustand                 │
│          Ant Design 5 + Axios                     │
├──────────────────────────────────────────────────┤
│               RESTful API (JSON)                  │
├──────────────────────────────────────────────────┤
│           Node.js Express.js 后端服务              │
│   ┌──────┬────────┬──────────┬──────┬─────────┐  │
│   │ 鉴权  │内容服务  │检测引擎   │审核流  │报表导出  │  │
│   │JWT   │CRUD    │AC自动机   │状态机  │docx      │  │
│   └──────┴────────┴──────────┴──────┴─────────┘  │
│          Prisma ORM / TypeORM                     │
├──────────────────────────────────────────────────┤
│                MySQL 8.0 数据库                    │
│   ┌──────┬──────┬────────┬──────┬──────────┐     │
│   │用户表 │内容表 │敏感词库 │审核表 │操作日志表 │     │
│   └──────┴──────┴────────┴──────┴──────────┘     │
└──────────────────────────────────────────────────┘
```

## 三、技术栈详表

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| **运行环境** | Node.js | ≥18 LTS | 稳定长期支持版本，npm 生态丰富 |
| **后端框架** | Express.js | 4.x | 生态最成熟、中间件丰富、学习曲线平缓 |
| **后端语言** | TypeScript | 5.x | 类型安全，与前端统一语言，减少联调错误 |
| **前端框架** | React | 18.x | 生态最大，Ant Design 深度适配 |
| **UI 组件库** | Ant Design | 5.x | 中文企业级组件库，表格/表单/审核流开箱即用 |
| **路由管理** | React Router | 6.x | React 官方路由方案，支持嵌套布局 |
| **状态管理** | Zustand | 4.x | 轻量级（<1KB），TypeScript 友好，无 Provider |
| **HTTP 客户端** | Axios | 1.x | 请求/响应拦截器、统一错误处理 |
| **数据库** | MySQL | 8.0 | 关系型数据强关联，窗口函数支持，课程标配 |
| **ORM** | Prisma | 5.x | 类型安全、自动迁移、Schema 可视化 |
| **敏感词检测** | aho-corasick | 1.x | AC 自动机，O(n) 一次扫描命中所有模式词 |
| **模式匹配** | 原生 RegExp | — | 手机号/身份证号/邮箱等格式检测 |
| **报表导出(Word)** | docx | 8.x | 纯 JS 生成 .docx 审核报告 |
| **报表导出(Excel)** | exceljs | 4.x | 纯 JS 生成 .xlsx 数据报表 |
| **鉴权** | jsonwebtoken + bcryptjs | — | JWT Token + 密码哈希，无状态认证 |
| **参数校验** | Zod | 3.x | TypeScript 优先的 schema 校验，可复用于前后端 |
| **构建工具** | Vite | 5.x | 极速 HMR，ESBuild 预编译 |
| **测试** | Jest + Supertest / Vitest | — | 后端接口测试 + 前端组件测试 |
| **版本控制** | Git + Gitee | — | 团队协作，代码审查 |
| **API 文档** | Swagger (swagger-jsdoc) | — | 自动生成接口文档，答辩展示加分 |

### 3.1 关键技术对比与决策依据

| 决策点 | 候选方案 | 最终选择 | 决策理由 |
|--------|---------|---------|----------|
| 后端框架 | Express / Koa / Fastify | **Express** | 社区资源最多、中间件生态最完善，适合教学场景 |
| ORM | Prisma / TypeORM / Sequelize | **Prisma** | 类型推导最强，Schema 即文档，迁移命令简洁 |
| 状态管理 | Redux / Zustand / MobX | **Zustand** | 无模板代码，API 极简，项目规模无需 Redux |
| 前端构建 | Vite / Webpack / CRA | **Vite** | 冷启动 <300ms，CRA 已停止维护 |
| 参数校验 | Zod / Joi / class-validator | **Zod** | TS 类型推导、前后端共享 schema，体积最小 |

## 四、核心技术难点分析

### 4.1 敏感词检测引擎

```
┌─────────────────────────────────────────┐
│         敏感词检测引擎 (Node.js)          │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────┐    │
│  │   AC 自动机 (aho-corasick)       │    │
│  │   · 精确匹配：违禁词、政治敏感词   │    │
│  │   · 模糊匹配：拼音、谐音、拆字    │    │
│  │   · 时间复杂度：O(n)             │    │
│  └─────────────────────────────────┘    │
│  ┌─────────────────────────────────┐    │
│  │   正则规则引擎 (RegExp)           │    │
│  │   · 手机号: /1[3-9]\d{9}/g      │    │
│  │   · 身份证: /\d{17}[\dXx]/g      │    │
│  │   · 邮箱: /[\w.-]+@[\w.-]+\.\w+/g│    │
│  │   · IP、银行卡号等               │    │
│  └─────────────────────────────────┘    │
│  ┌─────────────────────────────────┐    │
│  │   风险评分模块                    │    │
│  │   命中词等级 × 数量 → 加权分数    │    │
│  │   0-30: 低风险  30-60: 中风险    │    │
│  │   60+: 高风险                    │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

**AC 自动机核心代码示例**（基于 `ahocorasick` npm 包）：

```typescript
import AhoCorasick from 'ahocorasick';

// 敏感词条目
interface SensitiveWord {
  word: string;
  risk: 'high' | 'medium' | 'low';
  replacement: string;
}

// 风险权重映射
const RISK_WEIGHT = { high: 40, medium: 15, low: 5 } as const;

class SensitiveDetector {
  private ac: AhoCorasick;
  private wordMap: Map<string, SensitiveWord>;

  constructor(words: SensitiveWord[]) {
    this.wordMap = new Map(words.map(w => [w.word, w]));
    // 构建 AC 自动机（服务启动时一次性构建）
    this.ac = new AhoCorasick(words.map(w => w.word));
  }

  /** 检测文本，返回命中结果与风险评分 */
  detect(text: string) {
    const hits = this.ac.search(text); // [[endIndex, [matchedWords]]]
    const matches = hits.flatMap(([_, words]) =>
      words.map(w => this.wordMap.get(w)!)
    );
    const score = matches.reduce(
      (sum, m) => sum + RISK_WEIGHT[m.risk], 0
    );
    const level = score >= 60 ? 'high' : score >= 30 ? 'medium' : 'low';
    return { matches, score, level };
  }

  /** 按策略过滤文本 */
  filter(text: string): string {
    const { matches } = this.detect(text);
    let result = text;
    for (const m of matches) {
      result = result.replaceAll(m.word, m.replacement || '***');
    }
    return result;
  }
}
```

### 4.2 内容过滤策略

| 策略 | 行为 | 适用风险等级 |
|------|------|:---:|
| **替换** | 将敏感词替换为 `***` 或自定义替换词 | 低 |
| **屏蔽** | 该条内容仅自己可见，不公开 | 中 |
| **提示** | 提交时弹窗警告，用户可选择修改 | 中 |
| **拒绝** | 直接拒绝发布，强制修改后重提交 | 高 |

### 4.3 审核流程状态机

```
   ┌──────────┐     提交审核     ┌──────────┐
   │  草稿     │ ─────────────→ │  待审核    │
   └──────────┘                 └────┬─────┘
                                     │
                         ┌───────────┴───────────┐
                         ↓                       ↓
                    ┌──────────┐           ┌──────────┐
                    │  已发布   │           │  已驳回   │
                    └──────────┘           └────┬─────┘
                                               │
                                               │ 修改重提交
                                               ↓
                                          ┌──────────┐
                                          │  待审核   │  (循环)
                                          └──────────┘
```

## 五、数据库核心表设计（概要）

| 表名 | 说明 | 核心字段 | 索引策略 |
|------|------|----------|----------|
| `users` | 用户表 | id, username, password_hash, role(admin/editor), created_at | username UNIQUE |
| `contents` | 内容表 | id, title, body, category, status, risk_level, author_id, created_at, updated_at | status + created_at 复合索引 |
| `sensitive_words` | 敏感词库 | id, word, risk_level, replacement, category, enabled | word UNIQUE, enabled 索引 |
| `reviews` | 审核记录 | id, content_id, reviewer_id, action(approve/reject), comment, created_at | content_id 索引 |
| `operation_logs` | 操作日志 | id, user_id, action, target_type, target_id, detail, ip, created_at | user_id + created_at 复合索引 |

> **说明**：`contents.status` 枚举值为 `draft / pending / published / rejected`；所有表主键使用自增 `INT`，时间字段默认 `CURRENT_TIMESTAMP`。

## 六、前端页面路由规划

| 路由 | 页面 | 权限 |
|------|------|:---:|
| `/login` | 登录页 | 公开 |
| `/dashboard` | 工作台/首页 | 登录用户 |
| `/contents` | 内容列表管理 | 编辑 |
| `/contents/create` | 发布新内容 | 编辑 |
| `/contents/:id` | 内容详情/预览 | 登录用户 |
| `/sensitive-words` | 敏感词库管理 | 管理员 |
| `/review` | 审核工作台 | 管理员 |
| `/review/:id` | 审核详情 | 管理员 |
| `/reports` | 审核报表 | 管理员 |
| `/logs` | 操作日志 | 管理员 |

## 七、项目目录结构

```
project-root/
├── client/                    # 前端 React 项目
│   ├── src/
│   │   ├── components/        # 公共组件
│   │   ├── pages/             # 页面组件
│   │   ├── hooks/             # 自定义 Hooks
│   │   ├── stores/            # Zustand 状态
│   │   ├── services/          # API 请求层
│   │   ├── types/             # TypeScript 类型
│   │   └── utils/             # 工具函数
│   ├── vite.config.ts
│   └── package.json
├── server/                    # 后端 Express 项目
│   ├── src/
│   │   ├── controllers/       # 路由控制器
│   │   ├── services/          # 业务逻辑
│   │   ├── middlewares/       # 中间件（鉴权、日志）
│   │   ├── models/            # 数据模型（Prisma/TypeORM）
│   │   ├── engine/            # 敏感词检测引擎
│   │   ├── routes/            # 路由定义
│   │   ├── utils/             # 工具函数
│   │   └── types/             # TypeScript 类型
│   ├── prisma/
│   │   └── schema.prisma      # 数据库 schema
│   └── package.json
├── shared/                    # 前后端共享类型
│   └── types/
├── docs/                      # 文档
│   ├── 需求分析.md
│   ├── 系统设计.md
│   └── 接口文档.md
└── README.md
```

## 八、团队分工建议（4–5人）

| 角色 | 人数 | 职责 | 对应技术栈 |
|------|:---:|------|------|
| **项目经理 + 后端(业务)** | 1 | 需求管理、数据库设计、内容CRUD、审核流程API、项目管理 | Express、Prisma、MySQL |
| **后端(检测引擎)** | 1 | 敏感词检测引擎、过滤策略、报表导出、操作日志 | aho-corasick、RegExp、docx、exceljs |
| **前端开发** | 1–2 | 全部页面开发、前后端联调、交互体验 | React、Ant Design、Zustand、Axios |
| **测试 + 文档** | 1 | 测试用例设计执行、实践报告撰写、答辩PPT | Jest、Vitest、Word |

## 九、开发阶段规划

| 阶段 | 周期 | 内容 | 产出 |
|------|:---:|------|------|
| 需求分析 | 第1周 | 梳理功能需求、用例图、需求文档 | 需求分析文档 |
| 系统设计 | 第1–2周 | 架构设计、数据库ER图、UML类图、接口设计 | 设计文档 |
| 技术选型与搭建 | 第2周 | 初始化项目脚手架、配置开发环境 | 项目骨架 |
| 编码实现 | 第2–4周 | 按模块并行开发，前后端对接 | 可运行系统 |
| 测试与优化 | 第4–5周 | 功能测试、边界测试、缺陷修复 | 测试报告 |
| 报告与答辩 | 第5周 | 撰写实践报告、制作答辩PPT、录制演示 | 报告 + PPT |

## 十、安全设计

| 安全领域 | 措施 | 说明 |
|---------|------|------|
| **认证** | JWT (Access + Refresh Token) | Access Token 有效期 15min，Refresh Token 7d，防泄露 |
| **密码** | bcryptjs (cost=10) | 单向哈希存储，禁止明文 |
| **权限** | RBAC (admin / editor) | 中间件统一拦截，路由级鉴权 |
| **输入校验** | Zod schema | 所有入参白名单校验，防注入 |
| **SQL 注入** | Prisma 参数化查询 | ORM 层自动转义，杜绝拼接 SQL |
| **XSS** | DOMPurify 前端过滤 + CSP 头 | 内容展示前净化 HTML |
| **CORS** | 白名单域名 | 仅允许前端域名跨域访问 |
| **日志脱敏** | 密码、Token 不入日志 | 操作日志仅记录行为摘要 |

## 十一、部署方案

```
开发环境 (本地):
  前端: npm run dev → Vite Dev Server (localhost:5173)
  后端: npx ts-node-dev → Express (localhost:3000)
  数据库: MySQL 8.0 本地实例

演示/答辩部署:
  方案A — 单机部署 (推荐):
    使用同一台服务器/笔记本，前端 build 后由 Express 托管静态文件
    nginx 反向代理 (可选)，一个端口对外服务

  方案B — 云服务器:
    前端 dist/ → nginx 静态托管
    后端 → pm2 守护进程
    MySQL → 云数据库 / Docker
```

## 十二、总结

本方案以 **Node.js + Express.js + React 18 + Ant Design 5 + MySQL + Prisma** 为核心技术栈，全栈 TypeScript 统一类型体系，降低团队协作与联调成本。核心亮点：

1. **敏感词检测**：AC 自动机 + 正则引擎混合策略，兼顾 O(n) 匹配效率与多模式覆盖
2. **类型安全**：前后端共享 Zod Schema + Prisma 类型推导，编译期消除接口不一致
3. **安全纵深**：JWT 鉴权 + RBAC 权限 + 参数化查询 + XSS 防护，多层防御
4. **工程规范**：ESLint + Prettier + Git 分支管理 + Swagger 文档，保障代码质量

该方案完全满足任务书选题 22 的全部功能要求，技术成熟度高，学习曲线适中，适合 4–5 人团队在 5 周内高质量交付。
